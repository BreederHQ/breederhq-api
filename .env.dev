# Development Runtime Environment
# Used by: npm run dev, npm run test

NODE_ENV=development
PORT=6001

# ── AWS Secrets Manager ──────────────────────────────────────────────────
# boot-dev.js fetches ALL secrets from SM at startup.
# Prerequisites: aws configure --profile dev (SSO auto-re-login on expiry)
USE_SECRETS_MANAGER=true
AWS_SECRET_NAME=breederhq/dev-prototype
AWS_PROFILE=dev
AWS_SECRETS_MANAGER_REGION=us-east-2

# ── Secrets fetched from SM (DO NOT duplicate here) ──────────────────────
# The following keys are pulled from Secrets Manager at boot:
#   DATABASE_URL, DATABASE_DIRECT_URL, COOKIE_SECRET,
#   RESEND_API_KEY, RESEND_WEBHOOK_SECRET, INBOUND_EMAIL_HMAC_SECRET,
#   STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET,
#   AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY,
#   JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, JWT_UNSUBSCRIBE_SECRET,
#   SENTRY_DSN,
#   FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY

# ── Local-only vars (NOT in Secrets Manager) ─────────────────────────────
# Feature flags
MARKETPLACE_PUBLIC_ENABLED=true

# Application URL (for emails)
APP_URL=http://localhost:5173

# Resend sender config (not secrets)
RESEND_FROM_EMAIL=noreply@mail.breederhq.com
RESEND_FROM_NAME=BreederHQ (Dev)
EMAIL_DEV_REDIRECT=dev@breederhq.com
RESEND_INBOUND_DOMAIN=mail.breederhq.com

# AWS config (not secrets — keys come from SM)
AWS_REGION=us-east-1
S3_BUCKET=breederhq-assets-dev
