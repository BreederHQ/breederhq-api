# Production Migration Environment
# Used by: npm run db:prod:* scripts (dbmate, prisma db pull, seed scripts)
#
# This file is a THIN CONFIG POINTER — no credentials here.
# Credentials are fetched from AWS Secrets Manager at runtime.
#
# run-with-env.js detects AWS_SECRET_NAME and:
#   1. Fetches the SM secret
#   2. Remaps DATABASE_URL = DATABASE_DIRECT_URL  (bhq_migrator @ direct — required for DDL)
#
# Available secrets (prod AWS account):
#   breederhq/prod-prototype  ← active prototype (ep-dark-breeze)
#   breederhq/prod            ← future production — use after migration cutover

NODE_ENV=production
AWS_SECRET_NAME=breederhq/prod-prototype
AWS_PROFILE=prod
AWS_SECRETS_MANAGER_REGION=us-east-2
