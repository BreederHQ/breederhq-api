########################################
# BreederHQ Environment Config Example #
########################################

# Copy to .env.dev for local development runtime
# Copy to .env.dev.migrate for local migrations
# Copy to .env.prod and .env.prod.migrate for prod work

# Runtime: Use pooled connection (bhq_app role)
# DATABASE_URL=postgresql://bhq_app:PASSWORD@HOST-pooler.../neondb?sslmode=require

# Migrations: Use direct connection (bhq_migrator role)
# DATABASE_URL=postgresql://bhq_migrator:PASSWORD@HOST.../neondb?sslmode=require
# DATABASE_DIRECT_URL=postgresql://bhq_migrator:PASSWORD@HOST.../neondb?sslmode=require

NODE_ENV=development
PORT=6001

# Session cookie signing (REQUIRED, min 32 chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
COOKIE_SECRET=

# Database connection
DATABASE_URL=

# Email (Resend)
RESEND_API_KEY=
RESEND_FROM_EMAIL=
RESEND_FROM_NAME=
