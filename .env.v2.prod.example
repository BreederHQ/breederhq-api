# Neon v2 Production Branch Environment
# Copy this file to .env.v2.prod and fill in the values.
#
# Get connection strings from Neon Console:
# 1. Select your v2 production project/branch
# 2. Go to Connection Details
# 3. Copy both pooled and direct connection strings
#
# IMPORTANT:
# - DATABASE_URL can use pooler (-pooler in hostname) for application queries
# - DATABASE_DIRECT_URL MUST use direct connection (no -pooler) for migrations
# - Use bhq_migrator role for DATABASE_DIRECT_URL (has migration privileges)
# - Never commit real credentials to git

# Pooled connection for application runtime (optional for migrations)
# Format: postgresql://USER:PASSWORD@HOST-pooler.../DATABASE?sslmode=require
DATABASE_URL=

# Direct connection for Prisma migrations (REQUIRED)
# Format: postgresql://bhq_migrator:PASSWORD@HOST/DATABASE?sslmode=require
# MUST NOT contain "-pooler" in the hostname
DATABASE_DIRECT_URL=
