# Neon v2 Production Branch Environment
# Copy this file to .env.v2.prod and fill in the values from Neon console.
#
# These credentials target the Neon v2 PROJECT, PRODUCTION branch.
# CAUTION: Only use for deploying migrations to production.
# Never run migrate dev against production.

# Pooled connection (for runtime via PgBouncer)
# Use role: bhq_app
# Format: postgresql://bhq_app:PASSWORD@HOST/DATABASE?sslmode=require&pgbouncer=true
DATABASE_URL=

# Direct connection (for migrations, bypasses PgBouncer)
# Use role: bhq_migrator
# Format: postgresql://bhq_migrator:PASSWORD@HOST/DATABASE?sslmode=require
DATABASE_DIRECT_URL=
