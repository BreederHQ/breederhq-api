# v1 Dev Snapshot Database Connection
# Copy this file to .env.v1.dev.snapshot and fill in the value.
#
# This is the DIRECT connection URL to the v1 dev database snapshot branch.
# Used only for pg_dump to export data.
#
# IMPORTANT: Use a snapshot branch, NOT the live v1 dev database.
# The snapshot should be created from v1 dev at a known point in time.

# Direct connection URL to v1 dev snapshot
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
V1_DEV_SNAPSHOT_DIRECT_URL=
