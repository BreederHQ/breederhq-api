# Development Runtime Environment
# Copy to .env.dev and fill in real values.
# Used by: npm run dev, npm run test

NODE_ENV=development
PORT=6001

# Database connection (pooled, for runtime queries)
# Use bhq_app role with pooler endpoint
DATABASE_URL=postgresql://bhq_app:PASSWORD@HOST-pooler.neon.tech/DATABASE?sslmode=require
