# Neon v2 Development Branch Environment
# Copy this file to .env.v2.dev and fill in the values from Neon console.
#
# These credentials target the Neon v2 PROJECT, DEVELOPMENT branch.
# Do NOT point to v1 databases or production.

# Pooled connection (for runtime via PgBouncer)
# Use role: bhq_app
# Format: postgresql://bhq_app:PASSWORD@HOST-pooler/DATABASE?sslmode=require
DATABASE_URL=

# Direct connection (for migrations, bypasses PgBouncer)
# Use role: bhq_migrator
# Format: postgresql://bhq_migrator:PASSWORD@HOST/DATABASE?sslmode=require
# Note: Prisma uses this for migrations and auto-creates/drops shadow databases.
DATABASE_DIRECT_URL=
