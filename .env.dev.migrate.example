# Development Migration Environment
# Copy to .env.dev.migrate and fill in real values.
# Used by: npm run db:v2:dev:migrate, npm run db:v2:dev:status

NODE_ENV=development

# Database connection (direct, for migrations)
# Use bhq_migrator role with direct endpoint (no -pooler)
DATABASE_URL=postgresql://bhq_migrator:PASSWORD@HOST.neon.tech/DATABASE?sslmode=require

# Direct URL for Prisma migrations (same as DATABASE_URL for migrations)
DATABASE_DIRECT_URL=postgresql://bhq_migrator:PASSWORD@HOST.neon.tech/DATABASE?sslmode=require
