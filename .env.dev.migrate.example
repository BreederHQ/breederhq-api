# Development Migration Environment
# Used by: npm run db:dev:migrate, npm run db:dev:status, npm run db:dev:grant

NODE_ENV=development

# Use bhq_migrator role with DIRECT endpoint (no -pooler in hostname)
DATABASE_URL=postgresql://bhq_migrator:PASSWORD@HOSTNAME.neon.tech/neondb?sslmode=require
DATABASE_DIRECT_URL=postgresql://bhq_migrator:PASSWORD@HOSTNAME.neon.tech/neondb?sslmode=require
