# Copy to .env.dev.migrate
# For prototype mode: set DATABASE_URL to bhq_proto connection
# For dev migrations: set DATABASE_URL to bhq_dev connection
# Use migrator role, not app role
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE_NAME?sslmode=require
