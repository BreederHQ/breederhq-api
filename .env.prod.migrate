# Production Prototype Migration Environment
# Used by: npm run db:prod:* scripts
#
# Credentials are loaded from AWS Secrets Manager at runtime.
# DATABASE_URL is automatically set to DATABASE_DIRECT_URL (bhq_migrator, direct endpoint).
#
# To switch to a different environment, change AWS_SECRET_NAME:
#   breederhq/prod-prototype  ← current prototype (ep-dark-breeze)
#   breederhq/prod            ← future production (flat-flower-54202261)

NODE_ENV=production
AWS_SECRET_NAME=breederhq/prod-prototype
AWS_PROFILE=prod
AWS_SECRETS_MANAGER_REGION=us-east-2
