# v1 Prod Snapshot Database Connection
# Copy this file to .env.v1.prod.snapshot and fill in the value.
#
# This is the DIRECT connection URL to the v1 prod database snapshot branch.
# Used only for pg_dump to export data.
#
# IMPORTANT: Use a snapshot branch, NOT the live v1 prod database.
# The snapshot should be created from v1 prod at a known point in time.
# NEVER copy directly from live prod - always use a snapshot.

# Direct connection URL to v1 prod snapshot
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
V1_PROD_SNAPSHOT_DIRECT_URL=
