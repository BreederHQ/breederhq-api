# Dev Prototype Migration Environment
# Used by: npm run db:dev:* scripts
#
# Credentials are loaded from AWS Secrets Manager at runtime.
# DATABASE_URL is automatically set to DATABASE_DIRECT_URL (bhq_migrator, direct endpoint).
#
# To switch to a different environment, change AWS_SECRET_NAME:
#   breederhq/dev-prototype  ← current prototype (ep-misty-frog)
#   breederhq/dev            ← future dev (polished-fire-14346254)

NODE_ENV=development
AWS_SECRET_NAME=breederhq/dev-prototype
AWS_PROFILE=dev
AWS_SECRETS_MANAGER_REGION=us-east-2
